interpolazione o valore in avanti per i nan

fare rolling con diversi parametri di overlap

analisi per snapshot è corretta

per stazionarietà: fare analisi da punto di partenza fino al primo evento rispettivamente di ww hpc o hpt in modo da isolare il profilo run to failure. L’analisi di variazione media e deviaizone standard va estesa anche ad altre feature.

vfuel ha valori di outlier, una kurtosis? piuttosto che uno shape factor. Calcoliamo anche le altre feature viste a lezione per vedere se una di esse porta informazione che ci può servire.

aumentiamo il numero delle feature per aumentare condition indicator per una regressione.

analisi in frequenza perde un po' di valore. tipo se lo facessimo per fan speed valuteremmo la frequenza dei valori. piuttosto istogram tra valore minimo e valore massimo da inizio a run to failure probabilistico.